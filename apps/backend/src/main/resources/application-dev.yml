spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}

logging:
  level:
    com.communityapp: DEBUG
    org.springframework.security: INFO

app:
  seed:
    sample-password: ${SEED_SAMPLE_PASSWORD:ChangeMe123!}
